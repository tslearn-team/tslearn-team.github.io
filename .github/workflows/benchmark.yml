name: Benchmark

on:
  workflow_dispatch:

jobs:

  benchmark:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
            python-version: "3.12"
        name: 'Setup python'
      - shell: bash
        run: |
          pip install pytest-benchmark[histogram]
          pip install tslearn[all_features]~=0.7.0
        name: 'Install tslearn'
      - shell: bash
        run: |
          pytest --benchmark-save=0.7
        name: 'Run benchmark'
      - shell: bash
        run: |
          pip install pytest-benchmark[histogram]
          pip install tslearn[all_features]~=0.8.0
        name: 'Install tslearn'
      - shell: bash
        run: |
          pytest --benchmark-save=0.8
        name: 'Run benchmark'
      - shell: bash
        run: |
          pip install pytest-benchmark[histogram]
          pip install 'tslearn[all_features] @ https://github.com/tslearn-team/tslearn/archive/main.zip'
        name: 'Install tslearn'
      - shell: bash
        run: |
          pytest --benchmark-save=latest
        name: 'Run benchmark'
      - shell: bash
        run: |
          pytest-benchmark compare --group-by func --histogram benchmark
        name: 'Compare benchmark'
      - name: Upload
        uses: actions/upload-artifact@v7
        with:
          path: benchmark*.svg
